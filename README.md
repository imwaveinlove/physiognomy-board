# 관상 분류기 @whitecotchi

캐릭터 사진을 넣으면 얼굴을 분석해서 **동물상**과 **^^ / ㅍㅍ상** 두 축으로 자동 분류해 주는 페이지.

## 쓰는 법

1. `index.html`을 브라우저로 열기
2. 사진을 드래그해서 놓기 (여러 장 한 번에 가능 · `Ctrl+V` 붙여넣기도 됨)
3. 자동으로 분석해서 보드에 배치 — 이름은 파일명으로 들어감
4. 결과가 어긋나면 얼굴을 다른 칸으로 **끌어다 놓으면** 바로 고쳐짐 (얼굴 클릭 → 모달에서 버튼으로도 가능)

얼굴을 클릭하면 뜨는 창에서:

- **이름**: 위쪽 이름칸을 그냥 눌러서 고쳐 쓰면 됨 (바로 저장)
- **사진 위치/크기**: 동그란 사진 위에서 **휠로 확대·축소**, **끌어서 위치 이동** (＋/− 버튼도 있음). 얼굴이 화면에 꽉 차게 맞추면 판정도 정확해짐 — 크롭을 바꾸면 잘라낸 영역으로 **자동 재분석**되고, 직접 지정해 둔 분류는 덮어쓰지 않음
- **삭제**: 삭제 버튼을 두 번 누르면 삭제 (실수 방지, 3초 지나면 해제)

보드 위 **전체 다시 분석** 버튼을 누르면 이미 올려둔 얼굴을 현재 기준으로 다시 판정한다 (분류 기준이 바뀌었을 때 재업로드 없이 반영용, 직접 지정한 분류는 유지).

## 분류

| 축 | 분류 |
|---|---|
| 표정 | `ㅍㅍ상` · `^^상` |
| 동물상 | 🐶 강아지상 · 🐱 고양이상 · 🦊 여우상 · 🐻 곰상 · 🐰 토끼상 · 🦌 사슴상 · 🐺 늑대상 · 🦖 공룡상 |

탭으로 두 축을 번갈아 볼 수 있고, 칩 오른쪽 위 뱃지에 반대 축 분류가 같이 표시됨.

## 얼굴궁합

`궁합` 탭에서 넣어둔 캐릭터끼리의 얼굴궁합을 본다. **베스트 궁합** 카드와 **전체 궁합표**(히트맵)가 나오고,
아무 칸이나 누르면 점수 구성과 한 줄 평이 뜬다. 점수는 세 가지를 합쳐서 낸다.

| 항목 | 비중 | 기준 |
|---|---|---|
| 동물상 상성 | 45% | 8×8 상성표. 결이 다른 조합(순한 쪽 × 날선 쪽)이 높고, 같은 상끼리는 닮은꼴이라 낮다 |
| 표정 조합 | 25% | `^^ 지수` 차이가 클수록 대비가 좋다. 둘 다 웃는상이면 덤 |
| 이목구비 밸런스 | 30% | 측정값 6종의 평균 차이. **너무 닮아도, 너무 달라도** 아쉽고 적당한 차이에서 최고점 |

점수는 구조상 58~98 구간에 모이고, 등급(`천생연분 / 찰떡궁합 / 좋은 짝 / 무난한 사이 / 각자 노는 사이`)은
그 실제 분포에 맞춰 끊었다. A×B와 B×A는 항상 같은 값이 나온다.

## 자동 판정 방식

브라우저에서 외부 라이브러리 없이 돌아가는 얼굴 인식 AI는 없고, 실사 얼굴 인식 모델은 일러스트 얼굴에 잘 맞지 않는다.
그래서 **이미지 픽셀을 직접 재는 방식**으로 구현했다.

1. HSV로 변환해 살색 픽셀 마스크를 만들고, 연결 성분 중 가장 크고 중앙에 가까운 덩어리를 얼굴로 잡음
2. 얼굴 박스 안에서 행별 폭을 재서 **얼굴 세로/가로 비율**, **턱선 테이퍼**(아래쪽 폭 / 중간 폭) 계산
3. 얼굴 위쪽 밴드에서 좌우 어두운 덩어리를 눈으로 잡아 **눈 가로세로비**, **눈 면적**, 윗선 기울기 회귀로 **눈꼬리 각도** 계산
4. 얼굴 아래쪽 밴드에서 가장 큰 어두운 연결 성분을 입으로 잡고, 윗선의 **양끝 높이 − 가운데 높이**로 입꼬리 곡률 계산
5. 얼굴 아래쪽 어두운 덩어리의 가로 폭으로 **입 너비**도 계산 (공룡상 판정의 핵심 값)
6. 위 값들을 정규화해 동물상 8종에 가중합 → softmax로 비율 산출
7. 입꼬리 곡률(주), 입 벌어짐, 전체 밝기/채도, 눈매 납작함을 합쳐 `tanh`로 **^^ 지수 0~100** 산출

모달의 "측정값" 칸에서 실제로 잰 숫자를 그대로 볼 수 있다.

### 정확도에 대해

기계가 픽셀만 보고 재는 값이라 사람이 보는 것만큼 정확하지 않다. 특히:

- 앞머리가 눈을 덮거나, 얼굴이 크게 기울어졌거나, 배경이 살색에 가까우면 얼굴 검출이 흔들림
- 눈이 거의 동그란 경우 눈꼬리 각도 신호가 약해짐 (원은 회전시켜도 윗선 기울기가 잘 안 변함)
- 입이 아주 작거나 머리카락에 가려지면 입꼬리를 못 재고, 이때는 밝기 위주로 판정 (모달에 "얼굴 인식 약함" 표시)

그래서 드래그로 고치는 기능이 기본으로 들어가 있다.

## 색상 모드

헤더의 스위치로 세 가지 중 고른다. 고른 모드는 `localStorage`에 남고, 다음에 열 때 첫 페인트 전에 적용돼 깜빡이지 않는다.

| 모드 | 톤 |
|---|---|
| **백코** (기본) | 연분홍 → 라일락 → 페리윙클 |
| **블루밍** | 하늘색 → 파스텔 민트 |
| **블루레몬** | 연노랑 → 옅은 물빛 → 푸른 바다 |

`ㅍㅍ상 / ^^상` 두 칸은 보드의 가장 큰 면이라 모드를 따라 색이 바뀌고,
동물상 8종 색은 종을 구분하는 값이라 모드와 무관하게 고정이다. PNG 내보내기도 현재 모드 색을 따른다.

## 디자인 · 아이콘

파스텔 드림코어 톤. 배경에 색 안개가 천천히 흐르고(`prefers-reduced-motion`이면 정지),
패널과 섹션은 반투명 유리(`backdrop-filter`)로 처리했다. 파비콘과 헤더 로고는 `icon/` 폴더의 픽셀 마스코트를 쓴다.

| 파일 | 용도 |
|---|---|
| `icon/icon-16.png`, `icon/icon-32.png` | 브라우저 탭 파비콘 |
| `icon/icon-180.png` | iOS 홈화면 아이콘 |
| `icon/icon-192.png` | 헤더 로고 |

## 광고 (Google AdSense)

| 넣은 것 | 위치 |
|---|---|
| 소유 확인 메타 태그 | `index.html` `<head>` |
| `adsbygoogle.js` 스크립트 | `index.html` `<head>` |
| `ads.txt` | 저장소 루트 |

스크립트만 있으면 **자동 광고(Auto ads)**가 동작한다 — 켜고 끄는 건 애드센스 대시보드에서 한다.
왼쪽 패널 아래에 수동 광고 자리(`#adBox`)를 미리 잡아뒀고, 애드센스에서 광고 단위를 만들어 받은
슬롯 ID를 `index.html` 의 `const AD_SLOT = ''` 에 넣으면 그 자리에 배너가 붙는다.
비어 있으면 자리 자체가 렌더되지 않으므로 빈 칸이 남지 않는다.

### ads.txt 위치 주의

크롤러는 **도메인 루트**의 `/ads.txt` 만 읽는다.
GitHub Pages 프로젝트 페이지(`imwaveinlove.github.io/physiognomy-board/`)로 올리면 이 파일은
`.../physiognomy-board/ads.txt` 에 놓여 인식되지 않는다.
`imwaveinlove.github.io` 저장소 루트에 같은 줄을 넣거나, 커스텀 도메인을 쓰고 그 루트에 두어야 한다.

## 구현 메모

- 외부 라이브러리 없는 단일 HTML 파일 (아이콘 이미지는 `icon/`, 애드센스 스크립트만 외부 요청)
- 색상 모드는 `:root` CSS 변수 + `data-theme="blooming"` 오버라이드 구조 (백코가 바닥값이라 스크립트 전에도 그려짐)
- 업로드 이미지는 최대 변 420px JPEG **원본 1장**으로 저장하고, 크롭은 `{cx, cy, s}` 값으로만 들고 있음
  (칩·모달은 `background-size`/`background-position`으로, 분석과 PNG 내보내기는 `drawImage`의 소스 사각형으로 같은 값을 씀)
- 분석은 크롭을 256px로 뽑아 160px로 줄여서 수행
- `prompt()`/`confirm()`은 쓰지 않음 — 크롬이 "추가 대화상자 표시 안 함"을 켜면 조용히 무시돼서 버튼이 먹통이 됨.
  이름은 인풋 직접 편집, 삭제는 2단계 버튼으로 처리
- `localStorage` 키: 보드 `gwansang-wc-v2`, 색상 모드 `physiognomy-theme`
- PNG 내보내기는 현재 보고 있는 탭 기준 (`^^/ㅍㅍ`는 2단 대비 레이아웃, `동물상`은 8단 스택, `궁합`은 상위 12쌍 카드)
